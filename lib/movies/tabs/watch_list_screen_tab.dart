import 'package:flutter/material.dart';

class WatchListScreenTab extends StatelessWidget {
  const WatchListScreenTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Watch Screen Tab', style: TextStyle(color: Colors.amber),));
  }
}