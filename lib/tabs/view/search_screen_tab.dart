import 'package:flutter/material.dart';

class SearchScreenTab extends StatelessWidget {
  const SearchScreenTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Search Screen Tab', style: TextStyle(color: Colors.amber),));
  }
}